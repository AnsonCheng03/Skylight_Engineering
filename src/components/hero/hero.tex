import { component$ } from "@builder.io/qwik";
import styles from "./hero.module.css";

export default component$((photos: any) => {
  return (
    <section class={styles.hero}>
      <div class={styles.heroSlideshow}>
        {Object.values(photos).map((photo: any) => {
          <div class={styles.heroSlideshowSlide}>
            <img src={photo.src} alt={photo.alt} />
          </div>;
        })}
      </div>
      <div class={styles.heroSlideshowControls}>
        <div class={styles.heroSlideshowControlsDots}></div>
      </div>
    </section>
  );
});
